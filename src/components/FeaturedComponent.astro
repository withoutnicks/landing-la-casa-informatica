---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<section class='py-6 px-4 bg-zinc-200 dark:bg-zinc-900'>
	<h2 class='text-3xl font-bold underline text-center'>{title}</h2>
	<div class='flex flex-wrap justify-center py-4 md:my-6 gap-4'>
		<slot />
	</div>
</section>
